@using EDM_DB
@model Applications.QuanLyTaiLieu.Dtos.GetList_TaiLieu_Output_Dto

@{
    string tableName = "tailieu";
    var columns = new[] {
       "Tên tài liệu", "Ngày tạo", "Ngày hết hạn", "Trạng thái thông báo"
    };

    bool themMoi = Model.ThaoTacs.Exists(x => x.MaThaoTac == "quanlytailieu-themmoi");
    bool capNhat = Model.ThaoTacs.Exists(x => x.MaThaoTac == "quanlytailieu-capnhat");
    bool xoaBo = Model.ThaoTacs.Exists(x => x.MaThaoTac == "quanlytailieu-xoabo");
}

<div class="mb-2 toggle-vis-bar">
    Hiển thị:
    @Public.Helpers.HtmlHelper.RenderToggleLinks($"{tableName}-getList", columns)
</div>
<table class="table table-hover table-bordered table-sticky w-100" id="@tableName-getList" data-page-length="10">
    <thead class="nowrap" style="white-space: nowrap !important">
        <tr>
            <th class="text-center" data-orderable="false"></th>

            @foreach (var c in columns)
            {
                <th class="text-center">@c</th>
            }

            <th class="text-center w-5" intro-container="dulieu-tacvu" data-orderable="false">
                <div style="white-space: nowrap">
                    <div class="btn-group btn-group-sm dropstart">
                        <button type="button" title="Thao tác" class="btn btn-primary dropdown-toggle dropdown-toggle-split"
                                data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"
                                data-reference="parent">
                            <i class="bi bi-gear-fill"></i>
                        </button>
                        <div class="dropdown-menu me-2" style="border: 1px solid black;">
                            @{
                                if (themMoi)
                                {
                                    <a class="dropdown-item c-pointer"
                                       onclick="quanLyTaiLieu.taiLieu.displayModal_CRUD('create')">
                                        <i class="bi bi-plus-circle-fill"></i> Thêm mới
                                    </a>
                                }
                                if (xoaBo)
                                {
                                    <a class="dropdown-item c-pointer"
                                       onclick="quanLyTaiLieu.taiLieu.delete('multiple')">
                                        <i class="bi bi-trash3-fill"></i> Xóa bỏ
                                    </a>
                                }
                            }
                        </div>
                    </div>
                </div>
            </th>
        </tr>
    </thead>
    <tfoot class="show-footer-above">
        <tr>
            <th class="text-center"><input class="form-check-input checkAll-@tableName-getList" type="checkbox" title="Chọn tất cả" /></th>

            @foreach (var c in columns)
            {
                <th><input type="text" class="form-control dt-search-col" placeholder="Tìm kiếm ..." /></th>
            }

            <th><input type="text" class="form-control dt-search-col" placeholder="Tìm kiếm ..." disabled /></th>
        </tr>
    </tfoot>
    <tbody>
        @foreach (var taiLieu in Model.TaiLieus)
        {
            <tr id="@taiLieu.TaiLieu.IdFile">
                <td class="text-center"><input class="form-check-input checkRow-@tableName-getList" type="checkbox" /></td>

                <td class="text-left">@((taiLieu.TaiLieu.FileName ?? "") + (string.IsNullOrWhiteSpace(taiLieu.TaiLieu.LoaiTep) ? "" : " - " + taiLieu.TaiLieu.LoaiTep))</td>
                <td class="text-center">@(taiLieu.TaiLieu.NgayTao?.ToString("dd/MM/yyyy"))</td>
                <td class="text-center">@(taiLieu.TaiLieu.NgayHetHan?.ToString("dd/MM/yyyy"))</td>
                <td class="text-center">@(
                    taiLieu.TaiLieu.TrangThaiBaoQuan.HasValue
                        ? (taiLieu.TaiLieu.TrangThaiBaoQuan == 1 ? "Đã thông báo" : taiLieu.TaiLieu.TrangThaiBaoQuan == 2 ? "Đang bảo quản" : taiLieu.TaiLieu.TrangThaiBaoQuan.ToString())
                        : "")
                </td>

                <td class="text-center">
                    <div style="white-space: nowrap">
                        <div class="btn-group btn-group-sm dropstart">
                            <button type="button" title="Thao tác" class="btn btn-primary dropdown-toggle dropdown-toggle-split"
                                    data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"
                                    data-reference="parent">
                                <i class="bi bi-gear-fill"></i>
                            </button>
                            <div class="dropdown-menu me-2" style="border: 1px solid black;">
                                @{
                                    if (capNhat)
                                    {
                                        <a class="dropdown-item c-pointer"
                                           onclick="quanLyTaiLieu.taiLieu.displayModal_CRUD('update', '@taiLieu.TaiLieu.IdFile')">
                                            <i class="bi bi-eye"></i> Cập nhật
                                        </a>
                                    }
                                    if (xoaBo)
                                    {
                                        <a class="dropdown-item c-pointer"
                                           onclick="quanLyTaiLieu.taiLieu.delete('single', '@taiLieu.TaiLieu.IdFile')">
                                            <i class="bi bi-trash3-fill"></i> Xóa bỏ
                                        </a>
                                    }
                                }
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>